const fs = require('node:fs');
const path = require('node:path');
const out = path.resolve('output/icon-transparency');
const ids = [...fs.readFileSync('src/data/ui/gameIconArt.ts', 'utf8').matchAll(/^  (\w+):/gm)].map(m => m[1]);
const image = (id, transparent, size) => `<img width="${size}" height="${size}" src="../../assets/artwork-v2/icons/${transparent ? 'game-transparent' : 'game'}/${id}.png" alt="${id}">`;
fs.mkdirSync(out, {recursive: true});
fs.writeFileSync(path.join(out, 'index.html'), `<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>UI icon transparency review</title><style>
*{box-sizing:border-box}body{margin:0;padding:28px;background:#101416;color:#f5ecd9;font:15px system-ui}h1{margin:0 0 8px;color:#d5aa55}p{max-width:850px;line-height:1.6}.controls{display:flex;gap:8px;flex-wrap:wrap;margin:20px 0}button{padding:10px 16px;color:inherit;background:#293339;border:1px solid #d5aa55;cursor:pointer}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(240px,1fr));gap:14px}article{border:1px solid #455055;padding:12px;background:#1c2427}h2{font-size:15px;text-transform:capitalize;margin:0 0 12px}.pair{display:grid;grid-template-columns:1fr 1fr;gap:8px}.stage{display:flex;align-items:center;justify-content:center;min-height:112px;background:var(--stage,#26363b)}.stage img{object-fit:contain;max-width:100%}.label{display:block;font-size:12px;color:#d5aa55;margin:6px 0}.small{display:flex;align-items:center;gap:16px;justify-content:center;padding:12px;background:var(--stage,#26363b)}.small img{object-fit:contain}a{color:#d5aa55}.nav{display:flex;max-width:390px;background:#1c2427;padding:8px;gap:4px}.nav div{flex:1;text-align:center;padding:8px 2px;border:1px solid #455055}.nav span{display:block;font-size:10px;margin-top:4px}
</style><h1>UI icons without black squares</h1><p>Original artwork is shown on the left; the transparent runtime version is on the right. The small samples use 17, 29 and 42 pixels. Use the background controls to inspect the cutout edges. Portrait backgrounds and full skill illustrations are outside this shared UI-icon pass.</p><div class="controls"><button onclick="document.body.style.setProperty('--stage','#26363b')">Slate panel</button><button onclick="document.body.style.setProperty('--stage','#e8dfc8')">Light parchment</button><button onclick="document.body.style.setProperty('--stage','#000')">Black</button><button onclick="document.body.style.setProperty('--stage','#5c7580')">Blue grey</button></div><h2>Navigation at game size</h2><div class="nav">${['guild','quests','world','heroes','inventory'].map(id=>`<div>${image(id,true,29)}<span>${id}</span></div>`).join('')}</div><p><a href="screens/GuildScreen--Narrow--320-top.png">320px game screenshot</a> · <a href="screens/GuildScreen--HighContrast--390-top.png">High contrast game screenshot</a></p><div class="grid">${ids.map(id=>`<article><h2>${id.replaceAll('_',' ')}</h2><div class="pair"><div><span class="label">Before</span><div class="stage">${image(id,false,104)}</div></div><div><span class="label">After</span><div class="stage">${image(id,true,104)}</div></div></div><span class="label">In-game sizes · 17 / 29 / 42</span><div class="small">${[17,29,42].map(s=>image(id,true,s)).join('')}</div></article>`).join('')}</div></html>`);
console.log(`Created review gallery for ${ids.length} shared UI icons.`);
