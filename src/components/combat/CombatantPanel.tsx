import React from "react";
import { StyleSheet, Text, View } from "react-native";
export function CombatantPanel({ name, nameColor, currentHP, maxHP, resource }: { name: string; nameColor?: string; currentHP: number; maxHP: number; resource?: string }) { const ratio = Math.max(0, Math.min(1, currentHP / maxHP)); return <View style={styles.panel}><Text style={[styles.name, nameColor ? { color: nameColor } : undefined]}>{name}</Text><View style={styles.track}><View style={[styles.fill, { width: `${ratio * 100}%` }]} /></View><Text style={styles.detail}>HP {Math.round(currentHP)} / {Math.round(maxHP)}{resource ? ` · ${resource}` : ""}</Text></View>; }
const styles = StyleSheet.create({ panel: { padding: 10, backgroundColor: "#1d2637", borderRadius: 9, borderWidth: 1, borderColor: "#3f4a60" }, name: { color: "#fff", fontWeight: "900" }, track: { height: 7, borderRadius: 5, backgroundColor: "#493039", marginTop: 7, overflow: "hidden" }, fill: { height: "100%", backgroundColor: "#59bd79" }, detail: { color: "#aab4c5", fontSize: 12, marginTop: 4 } });
