import React from "react"; import { ScrollView, StyleSheet, Text, View } from "react-native"; import { colors } from "../ui";
export function TurnOrder({ ids, cursor, labels, labelColors, aliveIds }: { ids: string[]; cursor: number; labels: Record<string, string>; labelColors: Record<string, string>; aliveIds: Set<string> }) { return <ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={styles.row}>{ids.map((id, index) => <View key={`${id}-${index}`} style={[styles.unit, index === cursor && styles.current, !aliveIds.has(id) && styles.dead]}><Text style={styles.number}>{index + 1}</Text><Text numberOfLines={1} style={[styles.label, labelColors[id] ? { color: labelColors[id] } : undefined]}>{labels[id] ?? id}</Text></View>)}</ScrollView>; }
const styles = StyleSheet.create({ row: { gap: 6, paddingVertical: 8 }, unit: { width: 83, padding: 7, borderWidth: 1, borderColor: colors.border, backgroundColor: colors.panel, borderRadius: 8 }, current: { borderColor: colors.gold, backgroundColor: colors.panel2 }, dead: { opacity: .35 }, number: { color: colors.gold, fontSize: 10, fontWeight: "900" }, label: { color: colors.text, fontSize: 11, fontWeight: "700", marginTop: 2 } });
