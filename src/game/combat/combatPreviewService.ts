import { calculateSkillDamage } from "./damageCalculator";
import { getModifiedCombatStat } from "./modifierService";
import type { CombatStats, CombatUnit } from "./combatTypes";
import type { CombatSkillDefinition } from "./skillTypes";

const stats=(unit:CombatUnit):CombatStats=>({...unit.stats,physicalDamage:getModifiedCombatStat(unit,"physicalDamage"),physicalDefense:getModifiedCombatStat(unit,"physicalDefense"),magicDamage:getModifiedCombatStat(unit,"magicDamage"),magicDefense:getModifiedCombatStat(unit,"magicDefense"),physicalAttackBonus:getModifiedCombatStat(unit,"physicalAttackBonus"),magicAttackBonus:getModifiedCombatStat(unit,"magicAttackBonus"),armorClass:getModifiedCombatStat(unit,"armorClass"),magicDefenseScore:getModifiedCombatStat(unit,"magicDefenseScore"),attackRollModifier:getModifiedCombatStat(unit,"attackRollModifier"),rangedAttackRollModifier:getModifiedCombatStat(unit,"rangedAttackRollModifier")});
export interface CombatAttackPreview{hitChance:number;normalDamage:number;criticalDamage:number;attackBonus:number;targetDefense:number;favoredDamageType:"physical"|"magic"|"balanced"}
export function getCombatAttackPreview(attacker:CombatUnit,target:CombatUnit,skill:CombatSkillDefinition):CombatAttackPreview|null{if(!skill.damageType||skill.damageMultiplier===undefined)return null;const a=stats(attacker),d=stats(target);const ranged=(skill.range??1)>1?a.rangedAttackRollModifier:0;const attackBonus=(skill.damageType==="magic"?a.magicAttackBonus:a.physicalAttackBonus)+a.attackRollModifier+ranged+(skill.attackRollModifier??0);const targetDefense=skill.damageType==="magic"?d.magicDefenseScore:d.armorClass;let hits=0;for(let roll=1;roll<=20;roll++)if(roll===20||(roll!==1&&roll+attackBonus>=targetDefense))hits++;const physicalProtection=d.armorClass+d.physicalDefense/10;const magicalProtection=d.magicDefenseScore+d.magicDefense/10;return{hitChance:hits/20,normalDamage:calculateSkillDamage(a,d,skill),criticalDamage:calculateSkillDamage(a,d,skill,true),attackBonus,targetDefense,favoredDamageType:Math.abs(physicalProtection-magicalProtection)<1?"balanced":physicalProtection<magicalProtection?"physical":"magic"};}
